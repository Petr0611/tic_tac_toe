# Project README

Tic-Tac-Toe

## Project Description

A brief description of the project.

## Technologies

- TypeScript
- Redux Toolkit
- Other technologies...

## Project Structure

- `gameSlice.ts` — describes the game slice.
- `store.ts` — Redux store configuration.
- `constants.ts` — project constants.

## Installation and Run

```bash
git clone <repo-url>
cd <project>
npm install
npm start
```

## Scripts

- `npm start` — run the project.
- `npm run build` — build the project.

## Functionality

Describe the functionality related to the files:

- Game state logic in `gameSlice.ts`.
- Redux store setup in `store.ts`.
- Level and game constants in `constants.ts`.
